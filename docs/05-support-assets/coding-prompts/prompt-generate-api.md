# Prompt: Generate API

```text
Use the existing FRD feature and A4 model as source of truth.
Generate exactly one A3 file.
List request fields, response fields, validations, process flow, cross-module contracts, and errors.
Reference existing tests or propose the needed coverage.
Do not expose repository methods as module-facing behavior.
Name ports, facades, or query services explicitly when cross-module collaboration exists.
```
