# Prompt: Generate Domain

```text
Use BRD and FRD as business context.
Define the aggregate root first.
Use ids for external references.
Document invariants, lifecycle, repository contract, and persistence assumptions.
```
