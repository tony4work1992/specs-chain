# Test Naming Conventions

- Prefix codes with module, action, and level, for example `ORD_CREATE_UNIT_001`.
- Keep descriptions business-readable and explicit.
- Prefer scenario-focused file names such as `order-lifecycle.e2e-test.yaml`.
