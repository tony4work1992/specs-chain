# Shared Data Model

> This file documents database schemas/collections that are shared across multiple features.

## Purpose

Define the system-wide data entities that multiple features depend on:
- User / Account schema (used by Auth, Order, Dashboard, etc.)
- Permission / Role schema
- Audit log schema
- Configuration / Settings schema
- Any shared lookup tables or reference data

## Shared Schemas

_To be documented as shared entities are identified._
